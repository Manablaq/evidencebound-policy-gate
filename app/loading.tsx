export default function Loading() {
  return <main className="error-screen" aria-busy="true"><section className="error-card"><div className="eyebrow">EVIDENCEBOUND / LOADING</div><h1>Opening the trust boundary.</h1><p>Preparing the EvidenceBound workspace. Public Bradbury reads will appear as soon as the interface is ready.</p></section></main>;
}
